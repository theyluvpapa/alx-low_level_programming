#include "main.h"
/**
 * _abs - Check main
 * @num: integer input
 * Description: Computes absolute value of integer
 * Return: 0
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (num * -1);
	}
}
